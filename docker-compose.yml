services:
  ci: &ci
    build:
      context: .
      dockerfile: Dockerfile
    command: ['composer', 'ci']
    environment:
      QUEUE_API_URL:
      STORAGE_TOKEN:

  dev:
    <<: *ci
    volumes:
      - ./:/code
